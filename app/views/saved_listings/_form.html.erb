<%= form_for(@saved_listing) do |f| %>
  <% if @saved_listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@saved_listing.errors.count, "error") %> prohibited this saved_listing from being saved:</h2>

      <ul>
      <% @saved_listing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :listing_id %><br>
    <%= f.number_field :listing_id %>
  </div>
  <div class="field">
    <%= f.label :sort_order %><br>
    <%= f.number_field :sort_order %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
